/**
 * The product-matching-processor backend module for the catalog plugin.
 *
 * Links Pyxis product listings with Konflux Applications via RPA repository
 * mappings, emitting bidirectional relations and auto-discovered System
 * entities into the Backstage catalog.
 *
 * @packageDocumentation
 */

export { catalogModuleProductMatching as default } from './module';
